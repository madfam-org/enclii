version: "2"

run:
  timeout: 5m
  go: "1.25"
  tests: false  # Skip linting test files (many false positives)

linters:
  default: none
  enable:
    - govet
    - ineffassign
    - unused
    - errcheck
    - staticcheck
  exclusions:
    presets:
      - std-error-handling
    rules:
      - linters:
          - staticcheck
        text: "^QF"
      - linters:
          - errcheck
        text: "\\.Close"

formatters:
  enable:
    - gofmt

issues:
  max-issues-per-linter: 20
  max-same-issues: 5
