{
  "name": "@enclii/docs-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "docusaurus": "docusaurus",
    "start": "docusaurus start --port 4203",
    "dev": "docusaurus start --port 4203",
    "build": "docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve --port 4203",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids"
  },
  "dependencies": {
    "@docusaurus/core": "3.10.2",
    "@docusaurus/preset-classic": "3.10.2",
    "@mdx-js/react": "^3.0.0",
    "clsx": "^2.1.0",
    "prism-react-renderer": "^2.3.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "redocusaurus": "^2.1.1"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "3.10.2",
    "@docusaurus/types": "3.10.2",
    "typescript": "^5.3.0"
  },
  "//overrides": "Transitive CVE remediation. This app is a standalone npm project (not a pnpm workspace member), so the pnpm.overrides in the repo-root package.json do not reach it; npm `overrides` is the equivalent idiom. Every entry stays inside the dependency's current major. Drop an entry once the upstream dependency ships the fixed range on its own.",
  "overrides": {
    "brace-expansion@^2.0.0": "^2.1.2",
    "fast-uri": "^3.1.4",
    "fast-xml-builder": "^1.1.7",
    "js-yaml": "^4.3.0",
    "lodash": "^4.18.0",
    "postcss": "^8.5.18",
    "ws": "^7.5.11"
  },
  "engines": {
    "node": ">=18.0"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  }
}
