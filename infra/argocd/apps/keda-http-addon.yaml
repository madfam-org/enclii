# KEDA HTTP Add-on
# Deploys: KEDA HTTP Add-on operator + interceptor + scaler
# Purpose: Enables HTTP-based scaling (scale-to-zero) for Functions runtime
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: keda-http-addon
  namespace: argocd
  labels:
    app.kubernetes.io/name: keda-http-addon
    app.kubernetes.io/part-of: enclii
    app.kubernetes.io/component: autoscaling
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://kedacore.github.io/charts
    chart: keda-add-ons-http
    targetRevision: 0.9.0  # Or the latest stable version
    helm:
      valueFiles: []
      values: |
        operator:
          replicaCount: 1
        interceptor:
          replicaCount: 1

  destination:
    server: https://kubernetes.default.svc
    namespace: keda

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - RespectIgnoreDifferences=true
