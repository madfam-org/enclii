apiVersion: v1
kind: Secret
metadata:
  name: argocd-notifications-secret
  namespace: argocd
type: Opaque
stringData:
  # SECURITY: Replace with actual secret before applying. Do NOT commit real values.
  # Generate: openssl rand -hex 32
  # Must match the value in enclii-argocd-webhook secret (enclii namespace)
  argocd-webhook-secret: REPLACE_BEFORE_APPLY
