apiVersion: v1
kind: Secret
metadata:
  name: argocd-notifications-secret
  namespace: argocd
type: Opaque
stringData:
  # SECURITY: This secret is applied on the cluster via kubectl (not committed to git).
  # Generate: openssl rand -hex 32
  # Must match the value in enclii-argocd-webhook secret (enclii namespace).
  # Applied: 2026-02-01 — both argocd and enclii namespace secrets are in sync.
  argocd-webhook-secret: REPLACE_BEFORE_APPLY
