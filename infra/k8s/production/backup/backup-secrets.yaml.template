---
# R2 Backup Credentials
#
# IMPORTANT: Do not commit this file with real values!
#
# 1. Copy this file: cp backup-secrets.yaml.template backup-secrets.yaml
# 2. Fill in your R2 credentials
# 3. Apply: kubectl apply -f backup-secrets.yaml
# 4. Add backup-secrets.yaml to .gitignore
#
# To create R2 credentials:
# 1. Go to Cloudflare Dashboard → R2
# 2. Create bucket: enclii-backups
# 3. Go to Manage R2 API Tokens
# 4. Create token with Object Read & Write permissions
# 5. Copy Account ID, Access Key ID, and Secret Access Key

apiVersion: v1
kind: Secret
metadata:
  name: r2-backup-credentials
  namespace: data
  labels:
    app.kubernetes.io/name: postgres-backup
    app.kubernetes.io/component: backup
    app.kubernetes.io/part-of: enclii
type: Opaque
stringData:
  # Your Cloudflare Account ID (found in R2 dashboard)
  account-id: "YOUR_CLOUDFLARE_ACCOUNT_ID"

  # R2 API Token Access Key ID
  access-key-id: "YOUR_R2_ACCESS_KEY_ID"

  # R2 API Token Secret Access Key
  secret-access-key: "YOUR_R2_SECRET_ACCESS_KEY"
