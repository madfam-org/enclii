# madfam-runners-deploy — the ADR-010a dedicated deploy scale set.
#
# HOW THIS FILE WAS PRODUCED (reproduce it the same way):
#   helm template arc-runner-deploy \
#     oci://ghcr.io/actions/actions-runner-controller-charts/gha-runner-scale-set \
#     --namespace arc-runners --version 0.14.0 \
#     -f infra/helm/arc/values-runner-set.yaml \
#     -f infra/helm/arc/values-runner-set-deploy.yaml
#
# then the three hand-edits runner-blue/rendered.yaml also carries, which are
# NOT expressible in the values files as they stand today:
#   1. ephemeral-storage requests/limits on the runner and dind containers —
#      without a request the scheduler scores a runner's disk as zero and
#      kubelet evicts whatever exceeds it once the node crosses its threshold;
#   2. the dind startupProbe timeoutSeconds;
#   3. the image DIGEST pin, twice (the runner container and the
#      init-dind-externals initContainer), because Kyverno's
#      require-image-digest rejects a floating tag.
#
# THE DIGEST PIN IS NOW IN TWO DIRECTORIES. Every base-image bump must repin
# runner-blue/rendered.yaml AND this file, or the deploy pool quietly keeps
# running the old agent and starts failing registration ~60 days later while
# blue looks healthy. It is pinned here at the same digest blue runs today.
---
# Source: gha-runner-scale-set/templates/manager_role.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: madfam-runners-deploy-gha-rs-manager
  namespace: arc-runners
  labels:
    helm.sh/chart: gha-rs-0.14.0
    app.kubernetes.io/name: madfam-runners-deploy
    app.kubernetes.io/instance: madfam-runners-deploy
    app.kubernetes.io/version: "0.14.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: gha-rs
    actions.github.com/scale-set-name: madfam-runners-deploy
    actions.github.com/scale-set-namespace: arc-runners
    app.kubernetes.io/component: manager-role
  annotations:
  finalizers:
    - actions.github.com/cleanup-protection
rules:
- apiGroups:
  - ""
  resources:
  - pods
  verbs:
  - create
  - delete
  - get
- apiGroups:
  - ""
  resources:
  - pods/status
  verbs:
  - get
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
- apiGroups:
  - ""
  resources:
  - serviceaccounts
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
- apiGroups:
  - rbac.authorization.k8s.io
  resources:
  - rolebindings
  verbs:
  - create
  - delete
  - get
  - patch
  - update
- apiGroups:
  - rbac.authorization.k8s.io
  resources:
  - roles
  verbs:
  - create
  - delete
  - get
  - patch
  - update
---
# Source: gha-runner-scale-set/templates/manager_role_binding.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: madfam-runners-deploy-gha-rs-manager
  namespace: arc-runners
  labels:
    helm.sh/chart: gha-rs-0.14.0
    app.kubernetes.io/name: madfam-runners-deploy
    app.kubernetes.io/instance: madfam-runners-deploy
    app.kubernetes.io/version: "0.14.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: gha-rs
    actions.github.com/scale-set-name: madfam-runners-deploy
    actions.github.com/scale-set-namespace: arc-runners
    app.kubernetes.io/component: manager-role-binding
  annotations:
  finalizers:
    - actions.github.com/cleanup-protection
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: madfam-runners-deploy-gha-rs-manager
subjects:
- kind: ServiceAccount
  name: 
    arc-controller
  namespace: 
    arc-system
---
# Source: gha-runner-scale-set/templates/autoscalingrunnerset.yaml
apiVersion: actions.github.com/v1alpha1
kind: AutoscalingRunnerSet
metadata:
  name: madfam-runners-deploy
  namespace: arc-runners
  labels:
    app.kubernetes.io/component: "autoscaling-runner-set"
    helm.sh/chart: gha-rs-0.14.0
    app.kubernetes.io/name: madfam-runners-deploy
    app.kubernetes.io/instance: madfam-runners-deploy
    app.kubernetes.io/version: "0.14.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: gha-rs
    actions.github.com/scale-set-name: madfam-runners-deploy
    actions.github.com/scale-set-namespace: arc-runners
  annotations:
    actions.github.com/values-hash: 33c1e3d4755e587270abd872742e0f9ffaf2df085ab9e638bc66adf2a1c3874
    actions.github.com/cleanup-manager-role-binding: madfam-runners-deploy-gha-rs-manager
    actions.github.com/cleanup-manager-role-name: madfam-runners-deploy-gha-rs-manager

spec:
  githubConfigUrl: https://github.com/madfam-org
  githubConfigSecret: github-app-secret
  runnerScaleSetName: madfam-runners-deploy
  maxRunners: 2
  minRunners: 1
  listenerTemplate:
    spec:
      containers:
      - name: listener
        resources:
          limits:
            cpu: 100m
            memory: 128Mi
          requests:
            cpu: 50m
            memory: 64Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          runAsNonRoot: true
          runAsUser: 1000
          seccompProfile:
            type: RuntimeDefault

  template:
    metadata:
      labels:
        arc.enclii.dev/active: "true"
        arc.enclii.dev/pool: deploy
      annotations:
        arc.enclii.dev/deployed-at: ""
    spec:
      imagePullSecrets:
        - name: ghcr-credentials
      nodeSelector: 
        role: builder
      terminationGracePeriodSeconds: 
        300
      tolerations: 
        - effect: NoSchedule
          key: builder
          operator: Equal
          value: "true"
      restartPolicy: Never
      serviceAccountName: arc-runner
      initContainers:
      - name: init-dind-externals
        
        image: ghcr.io/madfam-org/enclii/arc-runner:stable@sha256:c35966ed277acedf16953c1e8075a2c9d92509be488b4de48b9487605da5a162
        command: ["cp"]
        args: ["-r", "/home/runner/externals/.", "/home/runner/tmpDir/"]
        volumeMounts:
          - name: dind-externals
            mountPath: /home/runner/tmpDir
      - name: dind
        image: docker:dind@sha256:6b9cd914eb9c6b342c040a49a27a5eb3804453bae6ecc90f7ff96133595a95e8
        args:
          - dockerd
          - --host=unix:///var/run/docker.sock
          - --group=$(DOCKER_GROUP_GID)
        env:
          - name: DOCKER_GROUP_GID
            value: "123"
        resources:
          requests:
            # Same defect as the runner container: kubelet's eviction message
            # named BOTH ("Container dind was using 80Ki, request is 0"). The
            # figure is small because the shared `work` volume holds the bulk,
            # but a container with request 0 is always an eviction candidate.
            # Deliberately no cpu/memory request added here — the builder nodes
            # already sit at 94-95% CPU and changing their scheduling weight is
            # a separate decision from stopping disk evictions.
            ephemeral-storage: 2Gi
          limits:
            ephemeral-storage: 8Gi
        securityContext:
          privileged: true
        restartPolicy: Always
        startupProbe:
          exec:
            command:
              - docker
              - info
          initialDelaySeconds: 0
          failureThreshold: 24
          periodSeconds: 5
          timeoutSeconds: 3
        volumeMounts:
          - name: work
            mountPath: /home/runner/_work
          - name: dind-sock
            mountPath: /var/run
          - name: dind-externals
            mountPath: /home/runner/externals
      containers:
      - name: runner
        
        command: 
          - /home/runner/run.sh
        image: 
          ghcr.io/madfam-org/enclii/arc-runner:stable@sha256:c35966ed277acedf16953c1e8075a2c9d92509be488b4de48b9487605da5a162
        imagePullPolicy: 
          Always
        resources:
          limits:
            cpu: 1500m
            memory: 6Gi
            # Cap a runaway job's disk instead of letting it take the node — and
            # with it every other runner scheduled there. Raised 12Gi -> 16Gi
            # alongside the request: peak measured usage was 8.11Gi with a browser
            # download, apt packages and two service containers on one runner, and
            # 12Gi left too little headroom above that for a heavier E2E shard.
            ephemeral-storage: 16Gi
          requests:
            cpu: 400m
            memory: 2Gi
            # LOAD-BEARING. Without an ephemeral-storage REQUEST the scheduler
            # scores this pod's disk usage as zero, packs runners onto a node
            # without accounting for their checkouts, and kubelet then evicts
            # whichever pods exceed their request once the node crosses its
            # eviction threshold (~12Gi free). Every runner exceeds zero, so
            # every runner is a candidate.
            #
            # Measured 2026-08-07: four runners evicted from a builder node
            # inside 20 minutes, each reported by kubelet as
            # "Container runner was using ~3073816Ki, request is 0".
            #
            # This is what a dying runner looks like from GitHub's side: no step
            # is marked `failure`, the in-flight step and everything after are
            # recorded not-run, and logs return BlobNotFound because the runner
            # died before uploading. It is indistinguishable from a flaky test,
            # and it was misdiagnosed as one across several repositories.
            #
            # RAISED 6Gi -> 10Gi on 2026-08-07 after E2E runners kept dying while
            # every other job type survived. Measured live on that builder
            # through the kubelet summary API: concurrent runners were using 8.11,
            # 7.14, 6.81, 5.06, 2.69 and 0.90 Gi. The heavy ones EXCEED a 6Gi
            # request, and kubelet evicts pods over their request first — so the
            # Playwright E2E pods were structurally always the victims, which is
            # exactly the selectivity observed (three incidents in seven hours,
            # never Build, Lint or Security Scan).
            #
            # That node was at 184/225Gi used with 31.8Gi free — near the 10% hard
            # eviction threshold — with nine runners packed on it while
            # the general-purpose worker sat at 31% with 62Gi free. A 10Gi request covers
            # real peak usage AND thins packing density (~22 slots instead of 37),
            # so the node stops being driven into its own threshold.
            ephemeral-storage: 10Gi
        env:
          - name: DOCKER_HOST
            value: unix:///var/run/docker.sock
          - name: RUNNER_WAIT_FOR_DOCKER_IN_SECONDS
            value: "120"
        volumeMounts:
          - name: work
            mountPath: /home/runner/_work
          - name: dind-sock
            mountPath: /var/run
      
      volumes:
      - name: dind-sock
        emptyDir: {}
      - name: dind-externals
        emptyDir: {}
      
      - name: work
        emptyDir:
          # Bound the workspace so a runaway checkout fails ITS OWN step with a
          # clear disk error, instead of silently inflating the pod's ephemeral
          # usage until kubelet evicts the whole runner mid-job — the
          # BlobNotFound / no-step-marked-failure signature that reads as a flaky
          # test. Deliberately below the 16Gi container limit.
          sizeLimit: 14Gi
