# =============================================================================
# Dhanam Namespace ResourceQuota
# =============================================================================
# Protects against "noisy neighbor" resource exhaustion
# Ensures dhanam workloads stay within defined boundaries
# =============================================================================

apiVersion: v1
kind: ResourceQuota
metadata:
  name: dhanam-compute-limit
  namespace: dhanam
  labels:
    app.kubernetes.io/name: dhanam
    app.kubernetes.io/component: resource-quota
    app.kubernetes.io/managed-by: enclii
spec:
  hard:
    # CPU limits (increased for rolling updates with 2x dhanam-api replicas)
    requests.cpu: "2"
    limits.cpu: "4"
    # Memory limits (increased for rolling updates headroom)
    requests.memory: 3Gi
    limits.memory: 6Gi
