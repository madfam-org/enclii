# =============================================================================
# Status Pod NetworkPolicies (enclii namespace)
# =============================================================================
# Egress rules for status page pods in the enclii namespace.
# Pods: status-enclii (port 4204), status-madfam (port 4204)
# CronJobs: status-recorder-enclii, status-recorder-madfam
# Managed by enclii repo, applied via kubectl (no ArgoCD app for policies dir).
#
# NOTE: The enclii namespace does NOT have default-deny policies (too many
# services). These are targeted policies for status pods that need cross-
# namespace access (PostgreSQL in data namespace).
# =============================================================================

# Allow status pods egress to DNS, HTTP/HTTPS (service checks), and PostgreSQL
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: status-egress
  namespace: enclii
  labels:
    app.kubernetes.io/managed-by: enclii
    app.kubernetes.io/component: network-policy
spec:
  podSelector:
    matchExpressions:
      - key: app
        operator: In
        values:
          - status-enclii
          - status-madfam
  policyTypes:
    - Egress
  egress:
    # DNS resolution
    - ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    # HTTP (service health checks within cluster)
    - ports:
        - protocol: TCP
          port: 80
    # HTTPS (external service checks + Prometheus)
    - ports:
        - protocol: TCP
          port: 443
    # PostgreSQL in data namespace (status history)
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: data
      ports:
        - protocol: TCP
          port: 5432

# Allow recorder CronJob pods egress to DNS + status service (HTTP)
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: status-recorder-egress
  namespace: enclii
  labels:
    app.kubernetes.io/managed-by: enclii
    app.kubernetes.io/component: network-policy
spec:
  podSelector:
    matchExpressions:
      - key: app
        operator: In
        values:
          - status-recorder-enclii
          - status-recorder-madfam
  policyTypes:
    - Egress
  egress:
    # DNS resolution
    - ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    # HTTP to status service within namespace (port 80 via ClusterIP)
    - to:
        - podSelector:
            matchExpressions:
              - key: app
                operator: In
                values:
                  - status-enclii
                  - status-madfam
      ports:
        - protocol: TCP
          port: 4204
